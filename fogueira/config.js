// Deixe vazio para rodar em MODO DEMO (dados só no seu navegador, sem servidor).
// Para virar um app online de verdade, preencha com os dados do seu projeto Supabase
// (Project Settings > API). Veja o LEIA-ME.md.
export const CONFIG = {
  supabaseUrl: '',      // ex.: 'https://abcdefgh.supabase.co'
  supabaseAnonKey: ''   // a chave "anon public" (pode ficar no front-end)
};
